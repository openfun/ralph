version: '3.4'

services:
  ralph:
    build:
      context: .
      target: development
      args:
        DOCKER_USER: ${DOCKER_USER:-1000}
    user: ${DOCKER_USER:-1000}
    image: ralph:development
    environment:
      PYLINTHOME: /app/.pylint.d
    volumes:
      - .:/app
